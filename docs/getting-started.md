# Getting Started

VMange manages Linux hosts through outbound agents. Add a host, install the generated agent command on that machine, then use the dashboard for health, VirtualBox, Docker, scripts, and audit history.

## First steps
- Sign in with an administrator account.
- Add a host from Overview or Hosts.
- Run the generated install command on the target machine.
- Wait for the first heartbeat, then open the host page.

## Main areas
- Hosts show health, capabilities, uptime, maintenance tools, and self-removing agent deletion.
- Virtual Machines show live VirtualBox inventory and actions.
- Containers And Compose show Docker resources and saved stacks.
- Audit records queued actions and their results.
- Alarms track host thresholds and active notifications.

## Documentation map
- Installation explains supported deployment layouts.
- Host Agent Installation explains enrollment and agent files.
- Hosts covers capabilities, reboot, and Wake-on-LAN.
- Virtual Machines covers VM actions and details.
- Containers And Compose covers Docker and stack lifecycle.
- Audit And Logs explains command outcomes.
- Alarms And Notifications explains rules and mail.
- Troubleshooting collects the first checks when something does not look right.
